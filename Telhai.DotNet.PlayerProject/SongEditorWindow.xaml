<Window x:Class="Telhai.DotNet.PlayerProject.SongEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Song Editor" Height="420" Width="520">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Title:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Artist:" VerticalAlignment="Center" Margin="0,8,0,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Artist, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Album:" VerticalAlignment="Center" Margin="0,8,0,0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Album, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Artwork:" VerticalAlignment="Center" Margin="0,8,0,0"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ArtworkUrl}" Margin="0,8,0,0" IsReadOnly="True"/>
        <Button Grid.Row="3" Grid.Column="2" Content="Choose..." Margin="8,8,0,0" Padding="10,2"
                Command="{Binding ChooseImageCommand}"/>

        <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,12,0,12" BorderThickness="1" BorderBrush="Gray">
            <Image Stretch="Uniform" Source="{Binding ArtworkUrl}" />
        </Border>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Remove Song" Margin="0,0,8,0" Padding="12,6" Command="{Binding RemoveSongCommand}"/>
            <Button Content="Save" Margin="0,0,8,0" Padding="12,6" Command="{Binding SaveCommand}"/>
            <Button Content="Close" Padding="12,6" Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>
